#FHS= Filesystem Hierarchy Standard
#learning about linux file system
#sudo mv abc.sh /usr/local/bin/ABC #ABC is usr created directory
#sudo chown root:root /usr/local/bin/ABC #owned by root user and root group
#you don't need to put .sh linux already knows by #!/bin/bash
#you just need to run abc and it will run from any where like global variable
#to add in PATH where linux can run code with out directing you do use this:
#export PATH=/usr/loca/bin:$PATH #replace the pwd that you want
